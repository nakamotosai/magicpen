# runtime / local only
__pycache__/
*.py[cod]
*$py.class
.DS_Store
Thumbs.db
*.prev.*
.venv/
venv/

# console session garbage (never ship)
console/runs/
console/**/state.json
console/**/*.bak

# probe / scratch scripts (never ship; may embed keys)
scripts/_*.py
scripts/_*.md

# demo run artifacts (local smoke; not product path)
examples/**/runs/

# research dumps (local only; not product path)
archive/
holdouts/

# editor / OS
.idea/
.vscode/
*.swp
*~

# accidental secrets
.env
.env.*
!.env.example
*.pem
*.key
